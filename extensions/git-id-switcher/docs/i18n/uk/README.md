# Git ID Switcher

<table>
  <tr>
    <td align="center" valign="top" width="150">
      <img src="https://assets.nullvariant.com/nullvariant-vscode-extensions/extensions/git-id-switcher/images/icon.png" width="128" alt="Git ID Switcher">
    </td>
    <td>
      Перемикайтеся між кількома Git-ідентифікаторами одним кліком. Керуйте кількома обліковими записами GitHub, SSH-ключами, GPG-підписами та <b>автоматично застосовуйте ідентифікатор до Git-підмодулів</b>.
      <br><br>
      <a href="https://marketplace.visualstudio.com/items?itemName=nullvariant.git-id-switcher"><img src="https://img.shields.io/visual-studio-marketplace/v/nullvariant.git-id-switcher" alt="VS Code Marketplace"></a>
      <a href="https://open-vsx.org/extension/nullvariant/git-id-switcher"><img src="https://img.shields.io/open-vsx/v/nullvariant/git-id-switcher" alt="Open VSX Registry"></a>
      <a href="https://opensource.org/licenses/MIT"><img src="https://img.shields.io/badge/License-MIT-yellow.svg" alt="License: MIT"></a>
      <a href="https://securityscorecards.dev/viewer/?uri=github.com/nullvariant/nullvariant-vscode-extensions"><img src="https://api.securityscorecards.dev/projects/github.com/nullvariant/nullvariant-vscode-extensions/badge" alt="OpenSSF Scorecard"></a>
      <a href="https://www.bestpractices.dev/projects/11709"><img src="https://www.bestpractices.dev/projects/11709/badge" alt="OpenSSF Best Practices"></a>
      <a href="https://github.com/nullvariant/nullvariant-vscode-extensions/attestations"><img src="https://img.shields.io/badge/SLSA-Level_3-green" alt="SLSA 3"></a>
      <a href="https://github.com/nullvariant/nullvariant-vscode-extensions/actions/workflows/security.yml"><img src="https://github.com/nullvariant/nullvariant-vscode-extensions/actions/workflows/security.yml/badge.svg" alt="Security"></a>
      <a href="https://github.com/nullvariant/nullvariant-vscode-extensions/actions/workflows/ci.yml"><img src="https://github.com/nullvariant/nullvariant-vscode-extensions/actions/workflows/ci.yml/badge.svg" alt="CI"></a>
      <a href="https://codecov.io/gh/nullvariant/nullvariant-vscode-extensions"><img src="https://codecov.io/gh/nullvariant/nullvariant-vscode-extensions/graph/badge.svg" alt="codecov"></a>
      <a href="https://github.com/step-security/harden-runner"><img src="https://img.shields.io/badge/Harden--Runner-enabled-7037F5" alt="Harden-Runner"></a>
      <a href="../../DESIGN_PHILOSOPHY.md"><img src="https://img.shields.io/badge/🪨_Karesansui-Architecture-4a5568" alt="Karesansui Architecture"></a>
      <br>
      <a href="../../LANGUAGES.md"><img src="https://img.shields.io/badge/🌐_Languages-17+9_more-blue" alt="26 Languages"></a> <a href="../en/README.md">🇺🇸</a> <a href="../ja/README.md">🇯🇵</a> <a href="../zh-CN/README.md">🇨🇳</a> <a href="../zh-TW/README.md">🇹🇼</a> <a href="../ko/README.md">🇰🇷</a> <a href="../de/README.md">🇩🇪</a> <a href="../fr/README.md">🇫🇷</a> <a href="../es/README.md">🇪🇸</a> <a href="../pt-BR/README.md">🇧🇷</a> <a href="../it/README.md">🇮🇹</a> <a href="../ru/README.md">🇷🇺</a> <a href="../pl/README.md">🇵🇱</a> <a href="../tr/README.md">🇹🇷</a> <a href="../cs/README.md">🇨🇿</a> <a href="../hu/README.md">🇭🇺</a> <a href="../bg/README.md">🇧🇬</a> <b>🇺🇦</b> <a href="../eo/README.md">🌍</a> <a href="../haw/README.md">🌺</a> <a href="../ain/README.md">🐻</a> <a href="../ryu/README.md">🐉</a> <a href="../tok/README.md">✨</a> <a href="../tlh/README.md">🖖</a> <a href="../x-lolcat/README.md">🐱</a> <a href="../x-pirate/README.md">🏴‍☠️</a> <a href="../x-shakespeare/README.md">🎭</a>
    </td>
  </tr>
</table>

<br>

<img src="https://assets.nullvariant.com/nullvariant-vscode-extensions/extensions/git-id-switcher/images/demo-uk.png" width="600" alt="Демо">

## 🎯 Чому Git ID Switcher?

Хоча існує багато інструментів для зміни Git-ідентифікаторів, **Git ID Switcher** вирішує складні проблеми, які інші часто ігнорують:

1. **Кошмар підмодулів**: При роботі з репозиторіями, що містять підмодулі (теми Hugo, vendor-бібліотеки тощо), зазвичай потрібно вручну налаштовувати `git config user.name` для *кожного* підмодуля. Це розширення елегантно вирішує проблему, рекурсивно застосовуючи ваш ідентифікатор до всіх активних підмодулів.
2. **Обробка SSH та GPG**: Воно не просто змінює ваше ім'я; воно також перемикає SSH-ключі в agent та налаштовує GPG-підпис, щоб ви ніколи не робили коміт з неправильним підписом.

## Можливості

- **Підтримка підмодулів**: Автоматичне поширення ідентифікатора на Git-підмодулі
- **Керування SSH-ключами**: Автоматичне перемикання SSH-ключів у ssh-agent
- **Підтримка GPG-підпису**: Налаштування GPG-ключа для підпису комітів (опціонально)
- **Перемикання ідентифікатора одним кліком**: Миттєва зміна Git user.name та user.email
- **Інтеграція в рядок стану**: Завжди бачите поточний ідентифікатор
- **Детальні підказки**: Повна інформація з описом та SSH-хостом
- **Кросплатформеність**: Працює на macOS, Linux та Windows
- **Багатомовність**: Підтримує 17 мов

## 🌏 Про багатомовну підтримку

> **Я ціную існування меншин.**
> Я не хочу відкидати їх лише тому, що їх мало.
> Навіть якщо переклади не ідеальні, сподіваюся, ви відчуєте наш намір зрозуміти та виявити повагу до мов меншин.

Це розширення підтримує всі 17 мов, які підтримує VSCode. Крім того, для документації README ми кидаємо собі виклик, перекладаючи на мови меншин і навіть жартівливі мови.

Це не просто «глобальна підтримка» — це «повага до мовного різноманіття». І я буду радий, якщо це стане інфраструктурою, де коміти, що покращують світ, надходять від розробників з усього світу, долаючи мовні бар'єри.

---

## Швидкий старт

Типове налаштування для керування особистим обліковим записом та корпоративним обліковим записом (Enterprise Managed User).

### Крок 1: Підготуйте SSH-ключі

Спочатку створіть SSH-ключі для кожного облікового запису (пропустіть, якщо вже є):

```bash
# Особистий
ssh-keygen -t ed25519 -C "sasha.ivanenko@personal.example.com" -f ~/.ssh/id_ed25519_personal

# Робочий
ssh-keygen -t ed25519 -C "sasha.ivanenko@company.example.com" -f ~/.ssh/id_ed25519_work
```

Зареєструйте **публічний ключ** (файл `.pub`) кожного ключа у відповідному обліковому записі GitHub.

> **Примітка**: На GitHub реєструється `id_ed25519_personal.pub` (публічний ключ). `id_ed25519_personal` (без розширення) — це приватний ключ. Ніколи не діліться ним і не завантажуйте нікуди.

### Крок 2: Налаштуйте SSH

Відредагуйте `~/.ssh/config`:

```ssh-config
# Особистий обліковий запис GitHub (за замовчуванням)
Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_personal
    IdentitiesOnly yes

# Робочий обліковий запис GitHub
Host github-work
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_work
    IdentitiesOnly yes
```

### Крок 3: Налаштуйте розширення

Відкрийте налаштування розширення (`Cmd+,` / `Ctrl+,`) → знайдіть "Git ID Switcher" → натисніть "Редагувати в settings.json":

```json
{
  "gitIdSwitcher.identities": [
    {
      "id": "personal",
      "icon": "🏠",
      "name": "Саша Іваненко",
      "service": "GitHub",
      "email": "sasha.ivanenko@personal.example.com",
      "description": "Особисті проєкти",
      "sshKeyPath": "~/.ssh/id_ed25519_personal"
    },
    {
      "id": "work",
      "icon": "💼",
      "name": "Саша Іваненко",
      "service": "GitHub Робочий",
      "email": "sasha.ivanenko@company.example.com",
      "description": "Робочий обліковий запис",
      "sshKeyPath": "~/.ssh/id_ed25519_work",
      "sshHost": "github-work"
    }
  ],
  "gitIdSwitcher.defaultIdentity": "personal",
  "gitIdSwitcher.autoSwitchSshKey": true,
  "gitIdSwitcher.applyToSubmodules": true
}
```

### Крок 4: Використовуйте

1. Натисніть на іконку ідентифікатора в рядку стану (внизу праворуч)
2. Виберіть ідентифікатор
3. Готово! Git-конфігурацію та SSH-ключ перемкнуто.

<img src="https://assets.nullvariant.com/nullvariant-vscode-extensions/extensions/git-id-switcher/images/quickpick-uk.png" width="600" alt="Quick Pick">

### Використання SSH-псевдонімів хостів

При клонуванні репозиторіїв використовуйте хост, що відповідає вашому ідентифікатору:

```bash
# Для робочого ідентифікатора (використовує псевдонім github-work)
git clone git@github-work:company/repo.git

# Для особистого ідентифікатора (використовує github.com за замовчуванням)
git clone git@github.com:sivanenko/repo.git
```

---

## Опціонально: GPG-підпис

Якщо ви підписуєте коміти за допомогою GPG:

### Крок 1: Знайдіть ID вашого GPG-ключа

```bash
gpg --list-secret-keys --keyid-format SHORT
```

Приклад виводу:

```text
sec   ed25519/ABCD1234 2024-01-01 [SC]
      ...
uid         [ultimate] Саша Іваненко <sasha.ivanenko@personal.example.com>
```

ID ключа — `ABCD1234`.

### Крок 2: Додайте GPG-ключ до ідентифікатора

```json
{
  "gitIdSwitcher.identities": [
    {
      "id": "personal",
      "icon": "🏠",
      "name": "Саша Іваненко",
      "service": "GitHub",
      "email": "sasha.ivanenko@personal.example.com",
      "description": "Особисті проєкти",
      "sshKeyPath": "~/.ssh/id_ed25519_personal",
      "gpgKeyId": "ABCD1234"
    }
  ]
}
```

При перемиканні на цей ідентифікатор розширення налаштовує:

- `git config user.signingkey ABCD1234`
- `git config commit.gpgsign true`

---

## Повний приклад: 4 облікових записи з SSH + GPG

Ось повний приклад, що об'єднує все:

### Конфігурація SSH (`~/.ssh/config`)

```ssh-config
# Особистий обліковий запис (за замовчуванням)
Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_personal
    IdentitiesOnly yes

# Робочий обліковий запис
Host github-work
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_work
    IdentitiesOnly yes

# Bitbucket обліковий запис
Host bitbucket.org
    HostName bitbucket.org
    User git
    IdentityFile ~/.ssh/id_ed25519_bitbucket
    IdentitiesOnly yes
```

### Налаштування розширення

```json
{
  "gitIdSwitcher.identities": [
    {
      "id": "personal",
      "icon": "🏠",
      "name": "Саша Іваненко",
      "service": "GitHub",
      "email": "sasha.ivanenko@personal.example.com",
      "description": "Особисті проєкти",
      "sshKeyPath": "~/.ssh/id_ed25519_personal",
      "gpgKeyId": "PERSONAL1"
    },
    {
      "id": "work",
      "icon": "💼",
      "name": "Саша Іваненко",
      "service": "GitHub Робочий",
      "email": "sasha.ivanenko@company.example.com",
      "description": "Робочий обліковий запис",
      "sshKeyPath": "~/.ssh/id_ed25519_work",
      "sshHost": "github-work",
      "gpgKeyId": "WORK1234"
    },
    {
      "id": "bitbucket",
      "icon": "🪣",
      "name": "Саша Іваненко",
      "service": "Bitbucket",
      "email": "sasha.ivanenko@bitbucket.example.com",
      "description": "Bitbucket проєкти",
      "sshKeyPath": "~/.ssh/id_ed25519_bitbucket",
      "sshHost": "bitbucket.org"
    },
    {
      "id": "freelance",
      "icon": "🎯",
      "name": "Саша Іваненко",
      "service": "GitLab",
      "email": "sasha.ivanenko@freelance.example.com",
      "description": "Фриланс-проєкти"
    }
  ],
  "gitIdSwitcher.defaultIdentity": "personal",
  "gitIdSwitcher.autoSwitchSshKey": true,
  "gitIdSwitcher.applyToSubmodules": true
}
```

Примітка: Останній ідентифікатор (`freelance`) без SSH — він лише перемикає Git-конфігурацію. Це корисно, коли ви використовуєте різну інформацію про коммітера з одним обліковим записом GitLab.

---

## Довідник з конфігурації

### Властивості ідентифікатора

| Властивість   | Обов'язково | Опис                                                       |
| ------------- | ----------- | ---------------------------------------------------------- |
| `id`          | ✅          | Унікальний ідентифікатор (напр.: `"work"`, `"personal"`)   |
| `name`        | ✅          | Git user.name — відображається в комітах                   |
| `email`       | ✅          | Git user.email — відображається в комітах                  |
| `icon`        |             | Емодзі в рядку стану (напр.: `"🏠"`). Тільки один емодзі   |
| `service`     |             | Назва сервісу (напр.: `"GitHub"`, `"GitLab"`). Для UI      |
| `description` |             | Короткий опис у селекторі та підказці                      |
| `sshKeyPath`  |             | Шлях до приватного SSH-ключа (напр.: `"~/.ssh/id_ed25519_work"`) |
| `sshHost`     |             | Псевдонім SSH-хоста (напр.: `"github-work"`)               |
| `gpgKeyId`    |             | ID GPG-ключа для підпису комітів                           |

#### Обмеження відображення

- **Рядок стану**: Текст довше ~25 символів скорочується з `...`
- **`icon`**: Можна використовувати тільки один емодзі (графемний кластер). Декілька емодзі або довгий текст не дозволяються

### Глобальні налаштування

| Налаштування                      | За замовчуванням | Опис                                           |
| --------------------------------- | ---------------- | ---------------------------------------------- |
| `gitIdSwitcher.identities`        | Див. приклад     | Список конфігурацій ідентифікаторів            |
| `gitIdSwitcher.defaultIdentity`   | Див. приклад     | ID ідентифікатора за замовчуванням             |
| `gitIdSwitcher.autoSwitchSshKey`  | `true`           | Автоматично перемикати SSH-ключ                |
| `gitIdSwitcher.showNotifications` | `true`           | Показувати сповіщення при перемиканні          |
| `gitIdSwitcher.applyToSubmodules` | `true`           | Застосовувати ідентифікатор до підмодулів Git  |
| `gitIdSwitcher.submoduleDepth`    | `1`              | Макс. глибина для вкладених підмодулів (1-5)   |
| `gitIdSwitcher.includeIconInGitConfig` | `false`     | Записувати емодзі іконки в Git config `user.name` |
| `gitIdSwitcher.logging.fileEnabled` | `false` | Увімкнути журнал аудиту (перемикання ідентифікаторів, операції SSH, тощо) |
| `gitIdSwitcher.logging.filePath` | `""` | Шлях до файлу журналу (напр.: `~/.git-id-switcher/security.log`). Порожньо = шлях за замовчуванням |
| `gitIdSwitcher.logging.maxFileSize` | `10485760` | Макс. розмір файлу до ротації (байти, 1MB-100MB) |
| `gitIdSwitcher.logging.maxFiles` | `5` | Макс. кількість файлів журналу в ротації (1-20) |
| `gitIdSwitcher.logging.level` | `"INFO"` | Рівень логування: `DEBUG`/`INFO`/`WARN`/`ERROR`/`SECURITY`. Записує вибраний рівень і вище |
| `gitIdSwitcher.commandTimeouts` | `{}` | Користувацький таймаут для команд (мс, 1сек-5хв). Напр.: `{"git": 15000, "ssh-add": 10000}` |

#### Про `includeIconInGitConfig`

Контролює поведінку при встановленні поля `icon`:

| Значення | Поведінка |
|----------|-----------|
| `false` (за замовчуванням) | `icon` показується тільки в UI редактора. В Git config записується тільки `name` |
| `true` | В Git config записується `icon + name`. Емодзі залишається в історії комітів |

Приклад: `icon: "👤"`, `name: "Саша Іваненко"`

| includeIconInGitConfig | Git config `user.name` | Підпис коміту |
|------------------------|------------------------|---------------|
| `false` | `Саша Іваненко` | `Саша Іваненко <email>` |
| `true` | `👤 Саша Іваненко` | `👤 Саша Іваненко <email>` |

### Примітка: Базове налаштування (без SSH)

Якщо вам не потрібно перемикати SSH-ключі (напр., використовуючи різну інформацію про коммітера з одним обліковим записом GitHub), можна використовувати мінімальну конфігурацію:

```json
{
  "gitIdSwitcher.identities": [
    {
      "id": "personal",
      "icon": "🏠",
      "name": "Саша Іваненко",
      "email": "sasha.ivanenko@personal.example.com",
      "description": "Особисті проєкти"
    },
    {
      "id": "work",
      "icon": "💼",
      "name": "Саша Іваненко",
      "email": "sasha.ivanenko@company.example.com",
      "description": "Робочий обліковий запис"
    }
  ]
}
```

Це налаштування перемикає лише `git config user.name` та `user.email`.

---

## Як це працює

### Структура рівнів Git config

Git-конфігурація має три рівні, де нижні рівні перезаписуються вищими:

```text
Системний (/etc/gitconfig)
    ↓ перезаписує
Глобальний (~/.gitconfig)
    ↓ перезаписує
Локальний (.git/config)  ← найвищий пріоритет
```

**Git ID Switcher записує на рівні `--local` (локальний для репозиторію).**

Це означає:

- Зберігає ідентифікатор у `.git/config` кожного репозиторію
- Можна підтримувати різні ідентифікатори для кожного репозиторію
- Глобальні налаштування (`~/.gitconfig`) не змінюються

### Поведінка при перемиканні ідентифікатора

При перемиканні ідентифікатора розширення виконує (по порядку):

1. **Git-конфігурація** (завжди): Встановлює `git config --local user.name` та `user.email`
2. **SSH-ключ** (якщо задано `sshKeyPath`): Видаляє інші ключі з ssh-agent, додає обраний
3. **GPG-ключ** (якщо задано `gpgKeyId`): Встановлює `git config --local user.signingkey` та вмикає підпис
4. **Підмодулі** (якщо увімкнено): Застосовує конфігурацію до всіх підмодулів (за замовчуванням: глибина 1)

### Механізм поширення на підмодулі

Локальна конфігурація діє на рівні репозиторію, тому автоматично не застосовується до підмодулів.
Тому це розширення надає функцію поширення на підмодулі (деталі див. у «Розширено: Підтримка підмодулів»).

---

## Розширено: Підтримка підмодулів

Для складних репозиторіїв з Git-підмодулями керування ідентифікатором часто викликає проблеми. Якщо ви робите коміт у підмодулі, Git використовує локальну конфігурацію цього підмодуля, яка може використовувати глобальну конфігурацію (неправильний email!), якщо не налаштована явно.

**Git ID Switcher** автоматично виявляє підмодулі та застосовує до них обраний ідентифікатор.

```json
{
  "gitIdSwitcher.applyToSubmodules": true,
  "gitIdSwitcher.submoduleDepth": 1
}
```

- `applyToSubmodules`: Увімкнути/вимкнути цю функцію
- `submoduleDepth`: Наскільки глибоко йти?
  - `1`: Лише прямі підмодулі (найчастіший випадок)
  - `2+`: Вкладені підмодулі (підмодулі всередині підмодулів)

Це гарантує, що ваш ідентифікатор завжди правильний, незалежно від того, робите ви коміт в основному репозиторії чи у vendor-бібліотеці.

---

## Усунення несправностей

### SSH-ключ не перемикається?

1. Переконайтеся, що `ssh-agent` запущено:

   ```bash
   eval "$(ssh-agent -s)"
   ```

2. Перевірте правильність шляху до ключа:

   ```bash
   ls -la ~/.ssh/id_ed25519_*
   ```

3. На macOS додайте до Keychain один раз:

   ```bash
   ssh-add --apple-use-keychain ~/.ssh/id_ed25519_work
   ```

### Неправильний ідентифікатор при push?

1. Перевірте, що віддалений URL використовує правильний псевдонім хоста:

   ```bash
   git remote -v
   # Має показувати git@github-work:... для робочих репозиторіїв
   ```

2. Оновіть за потреби:

   ```bash
   git remote set-url origin git@github-work:company/repo.git
   ```

### GPG-підпис не працює?

1. Знайдіть ID вашого GPG-ключа:

   ```bash
   gpg --list-secret-keys --keyid-format SHORT
   ```

2. Протестуйте підпис:

   ```bash
   echo "test" | gpg --clearsign
   ```

3. Переконайтеся, що email у вашому ідентифікаторі збігається з email GPG-ключа.

### Ідентифікатор не визначається?

- Переконайтеся, що ви в Git-репозиторії
- Перевірте `settings.json` на синтаксичні помилки
- Перезавантажте вікно VS Code (`Cmd+Shift+P` → "Перезавантажити вікно")

### Помилка в полі `name`?

Поле `name` викликає помилку, якщо містить такі символи:

`` ` `` `$` `(` `)` `{` `}` `|` `&` `<` `>`

Якщо хочете додати назву сервісу, використовуйте поле `service`.

```jsonc
// Неправильно
"name": "Саша Іваненко (особистий)"

// Правильно
"name": "Саша Іваненко",
"service": "GitHub"
```

### Нові налаштування не відображаються?

Після оновлення розширення нові налаштування можуть не з'являтися на екрані налаштувань.

**Рішення:** Перезапустіть комп'ютер повністю.

VS Code та інші редактори зберігають схему налаштувань у пам'яті, і вона не завжди оновлюється після «Перезавантаження вікна» або перевстановлення розширення.

### Значення за замовчуванням (identities тощо) порожні?

Якщо зразкові налаштування не відображаються навіть при новій установці, причиною може бути **Settings Sync**.

Якщо раніше ви зберігали порожні налаштування, вони синхронізувалися в хмару і перезаписують значення за замовчуванням при новій установці.

**Рішення:**

1. Знайдіть налаштування на екрані налаштувань
2. Натисніть значок шестерні → «Скинути налаштування»
3. Синхронізуйте з Settings Sync (старі налаштування видаляться з хмари)

---

## Команди

| Команда                         | Опис                              |
| ------------------------------- | --------------------------------- |
| `Git ID: Select Identity`       | Відкрити селектор ідентифікаторів |
| `Git ID: Show Current Identity` | Показати поточний ідентифікатор   |

---

## Довіра робочого простору

Git ID Switcher підтримує функцію [Workspace Trust](https://code.visualstudio.com/docs/editor/workspace-trust) у VS Code. У недовірених робочих просторах небезпечні операції автоматично обмежуються.

### Поведінка в недовірених робочих просторах

| Операція                        | Поведінка                        |
| ------------------------------- | -------------------------------- |
| Перегляд ідентифікаторів        | ✅ Дозволено                     |
| Перегляд поточного Git config   | ✅ Дозволено                     |
| Перемикання ідентифікатора      | ⚠️ Потрібне підтвердження користувача |
| Зміна Git config                | ❌ Заблоковано                   |
| Операції з SSH-ключами          | ❌ Заблоковано                   |

### Вимкнення (не рекомендується)

Якщо вам конче потрібно вимкнути перевірку довіри робочого простору:

1. Відкрийте Налаштування (`Cmd+,` / `Ctrl+,`)
2. Знайдіть `gitIdSwitcher.disableWorkspaceTrust`
3. Увімкніть налаштування

⚠️ **Попередження**: Увімкнення цього налаштування знижує безпеку. Використовуйте лише коли це абсолютно необхідно.

---

## Філософія дизайну

> «Хто я?» — Єдине питання, на яке відповідає це розширення.

Побудовано на **Архітектурі Карэсансуй**: просте ядро (100 рядків),
оточене навмисною якістю (90% покриття, логування, таймаути)
та свідомими обмеженнями (без GitHub API, без управління токенами).

[![Karesansui Architecture](https://img.shields.io/badge/🪨_Karesansui-Architecture-4a5568)](../../DESIGN_PHILOSOPHY.md)

[Читати повну філософію](../../DESIGN_PHILOSOPHY.md)

---

## Участь у розробці

Внески вітаються! Див. [CONTRIBUTING.md](../../CONTRIBUTING.md).

## Ліцензія

Ліцензія MIT — див. [LICENSE](../../../LICENSE).

## Подяки

Створено [Null;Variant](https://github.com/nullvariant)
