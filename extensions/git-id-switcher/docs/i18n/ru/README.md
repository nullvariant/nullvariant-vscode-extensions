# Git ID Switcher

<table>
  <tr>
    <td align="center" valign="top" width="150">
      <img src="https://assets.nullvariant.com/nullvariant-vscode-extensions/extensions/git-id-switcher/images/icon.png" width="128" alt="Git ID Switcher">
    </td>
    <td>
      Переключайтесь между несколькими Git-идентификаторами одним кликом. Управляйте несколькими аккаунтами GitHub, SSH-ключами, GPG-подписью и <b>автоматически применяйте идентификатор к подмодулям Git</b>.
      <br><br>
      <a href="https://marketplace.visualstudio.com/items?itemName=nullvariant.git-id-switcher"><img src="https://img.shields.io/visual-studio-marketplace/v/nullvariant.git-id-switcher" alt="VS Code Marketplace"></a>
      <a href="https://open-vsx.org/extension/nullvariant/git-id-switcher"><img src="https://img.shields.io/open-vsx/v/nullvariant/git-id-switcher" alt="Open VSX Registry"></a>
      <a href="https://opensource.org/licenses/MIT"><img src="https://img.shields.io/badge/License-MIT-yellow.svg" alt="License: MIT"></a>
      <a href="https://securityscorecards.dev/viewer/?uri=github.com/nullvariant/nullvariant-vscode-extensions"><img src="https://api.securityscorecards.dev/projects/github.com/nullvariant/nullvariant-vscode-extensions/badge" alt="OpenSSF Scorecard"></a>
      <a href="https://www.bestpractices.dev/projects/11709"><img src="https://www.bestpractices.dev/projects/11709/badge" alt="OpenSSF Best Practices"></a>
      <a href="https://github.com/nullvariant/nullvariant-vscode-extensions/attestations"><img src="https://img.shields.io/badge/SLSA-Level_3-green" alt="SLSA 3"></a>
      <a href="https://github.com/nullvariant/nullvariant-vscode-extensions/actions/workflows/security.yml"><img src="https://github.com/nullvariant/nullvariant-vscode-extensions/actions/workflows/security.yml/badge.svg" alt="Security"></a>
      <a href="https://github.com/nullvariant/nullvariant-vscode-extensions/actions/workflows/ci.yml"><img src="https://github.com/nullvariant/nullvariant-vscode-extensions/actions/workflows/ci.yml/badge.svg" alt="CI"></a>
      <a href="https://github.com/nullvariant/nullvariant-vscode-extensions/actions/workflows/ci.yml"><img src="https://img.shields.io/badge/Windows-0078D6?logo=windows&logoColor=white" alt="Windows"></a>
      <a href="https://github.com/nullvariant/nullvariant-vscode-extensions/actions/workflows/ci.yml"><img src="https://img.shields.io/badge/macOS-000000?logo=apple&logoColor=white" alt="macOS"></a>
      <a href="https://github.com/nullvariant/nullvariant-vscode-extensions/actions/workflows/ci.yml"><img src="https://img.shields.io/badge/Linux-FCC624?logo=linux&logoColor=black" alt="Linux"></a>
      <a href="https://codecov.io/gh/nullvariant/nullvariant-vscode-extensions"><img src="https://codecov.io/gh/nullvariant/nullvariant-vscode-extensions/graph/badge.svg" alt="codecov"></a>
      <a href="https://sonarcloud.io/summary/new_code?id=nullvariant_nullvariant-vscode-extensions"><img src="https://sonarcloud.io/api/project_badges/measure?project=nullvariant_nullvariant-vscode-extensions&metric=alert_status" alt="Quality Gate Status"></a>
      <a href="https://github.com/step-security/harden-runner"><img src="https://img.shields.io/badge/Harden--Runner-enabled-7037F5" alt="Harden-Runner"></a>
      <a href="../../DESIGN_PHILOSOPHY.md"><img src="https://img.shields.io/badge/🪨_Karesansui-Architecture-4a5568" alt="Karesansui Architecture"></a>
      <br>
      <a href="../../LANGUAGES.md"><img src="https://img.shields.io/badge/🌐_Languages-17+9_more-blue" alt="26 Languages"></a> <a href="../en/README.md">🇺🇸</a> <a href="../ja/README.md">🇯🇵</a> <a href="../zh-CN/README.md">🇨🇳</a> <a href="../zh-TW/README.md">🇹🇼</a> <a href="../ko/README.md">🇰🇷</a> <a href="../de/README.md">🇩🇪</a> <a href="../fr/README.md">🇫🇷</a> <a href="../es/README.md">🇪🇸</a> <a href="../pt-BR/README.md">🇧🇷</a> <a href="../it/README.md">🇮🇹</a> <b>🇷🇺</b> <a href="../pl/README.md">🇵🇱</a> <a href="../tr/README.md">🇹🇷</a> <a href="../cs/README.md">🇨🇿</a> <a href="../hu/README.md">🇭🇺</a> <a href="../bg/README.md">🇧🇬</a> <a href="../uk/README.md">🇺🇦</a> <a href="../eo/README.md">🌍</a> <a href="../haw/README.md">🌺</a> <a href="../ain/README.md">🐻</a> <a href="../ryu/README.md">🐉</a> <a href="../tok/README.md">✨</a> <a href="../tlh/README.md">🖖</a> <a href="../x-lolcat/README.md">🐱</a> <a href="../x-pirate/README.md">🏴‍☠️</a> <a href="../x-shakespeare/README.md">🎭</a>
    </td>
  </tr>
</table>

<br>

<img src="https://assets.nullvariant.com/nullvariant-vscode-extensions/extensions/git-id-switcher/images/demo-ru.png" width="600" alt="Демо">

## 🎯 Почему Git ID Switcher?

Хотя существует много инструментов для смены Git-идентификаторов, **Git ID Switcher** решает сложные проблемы, которые другие часто игнорируют:

1. **Кошмар подмодулей**: При работе с репозиториями, содержащими подмодули (темы Hugo, vendor-библиотеки и т.д.), обычно приходится вручную настраивать `git config user.name` для *каждого* подмодуля. Это расширение элегантно решает проблему, рекурсивно применяя ваш идентификатор ко всем активным подмодулям.
2. **Обработка SSH и GPG**: Оно не просто меняет ваше имя; оно также переключает SSH-ключи в agent и настраивает GPG-подпись, чтобы вы никогда не делали коммит с неправильной подписью.

## Возможности

- **Поддержка подмодулей**: Автоматическое распространение идентификатора на подмодули Git
- **Управление SSH-ключами**: Автоматическое переключение SSH-ключей в ssh-agent
- **Поддержка GPG-подписи**: Настройка GPG-ключа для подписи коммитов (опционально)
- **Переключение идентификатора одним кликом**: Мгновенная смена Git user.name и user.email
- **Интеграция в статусную строку**: Всегда видите текущий идентификатор
- **Подробные подсказки**: Полная информация с описанием и SSH-хостом
- **Кроссплатформенность**: Работает на macOS, Linux и Windows
- **Многоязычность**: Поддержка 17 языков

## 🌏 О многоязычной поддержке

> **Я ценю существование меньшинств.**
> Я не хочу отбрасывать их только потому, что их мало.
> Даже если переводы не идеальны, я надеюсь, что вы почувствуете наше намерение понять и проявить уважение к языкам меньшинств.

Это расширение поддерживает все 17 языков, которые поддерживает VSCode. Кроме того, для документации README мы бросаем себе вызов, переводя на языки меньшинств и даже шуточные языки.

Это не просто «глобальная поддержка» — это «уважение к языковому разнообразию». И я буду рад, если это станет инфраструктурой, где коммиты, делающие мир лучше, приходят от разработчиков со всего мира, преодолевая языковые барьеры.

---

## Быстрый старт

Типичная настройка для управления личным аккаунтом и рабочим аккаунтом (Enterprise Managed User).

### Шаг 1: Подготовка SSH-ключей

Сначала создайте SSH-ключи для каждого аккаунта (пропустите, если уже есть):

```bash
# Личный
ssh-keygen -t ed25519 -C "sasha.ivanov@personal.example.com" -f ~/.ssh/id_ed25519_personal

# Рабочий
ssh-keygen -t ed25519 -C "sasha.ivanov@company.example.com" -f ~/.ssh/id_ed25519_work
```

Зарегистрируйте **публичный ключ** (файл `.pub`) каждого ключа в соответствующем аккаунте GitHub.

> **Примечание**: На GitHub регистрируется `id_ed25519_personal.pub` (публичный ключ). `id_ed25519_personal` (без расширения) — это приватный ключ. Никогда не делитесь им и не загружайте никуда.

### Шаг 2: Настройка SSH

Отредактируйте `~/.ssh/config`:

```ssh-config
# Личный аккаунт GitHub (по умолчанию)
Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_personal
    IdentitiesOnly yes

# Рабочий аккаунт GitHub (Enterprise Managed User от компании)
Host github-work
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_work
    IdentitiesOnly yes
```

### Шаг 3: Настройка расширения

Откройте настройки расширения (`Cmd+,` / `Ctrl+,`) → найдите "Git ID Switcher" → нажмите "Редактировать в settings.json":

```json
{
  "gitIdSwitcher.identities": [
    {
      "id": "personal",
      "icon": "🏠",
      "name": "Саша Иванов",
      "email": "sasha.ivanov@personal.example.com",
      "service": "GitHub",
      "description": "Личные проекты",
      "sshKeyPath": "~/.ssh/id_ed25519_personal"
    },
    {
      "id": "work",
      "icon": "💼",
      "name": "Саша Иванов",
      "email": "sasha.ivanov@company.example.com",
      "service": "GitHub Работа",
      "description": "Рабочий аккаунт",
      "sshKeyPath": "~/.ssh/id_ed25519_work",
      "sshHost": "github-work"
    }
  ],
  "gitIdSwitcher.defaultIdentity": "personal",
  "gitIdSwitcher.autoSwitchSshKey": true,
  "gitIdSwitcher.applyToSubmodules": true
}
```

### Шаг 4: Использование

1. Нажмите на иконку идентификатора в статусной строке (внизу справа)
2. Выберите идентификатор
3. Готово! Git-конфигурация и SSH-ключ переключены.

<img src="https://assets.nullvariant.com/nullvariant-vscode-extensions/extensions/git-id-switcher/images/quickpick-ru.png" width="600" alt="Quick Pick">

### Использование SSH-алиасов хостов

При клонировании репозиториев используйте хост, соответствующий вашему идентификатору:

```bash
# Для рабочего идентификатора (использует алиас github-work)
git clone git@github-work:company/repo.git

# Для личного идентификатора (использует github.com по умолчанию)
git clone git@github.com:sivanov/repo.git
```

---

## Опционально: GPG-подпись

Если вы подписываете коммиты с помощью GPG:

### Шаг 1: Найти ID вашего GPG-ключа

```bash
gpg --list-secret-keys --keyid-format SHORT
```

Пример вывода:

```text
sec   ed25519/ABCD1234 2024-01-01 [SC]
      ...
uid         [ultimate] Саша Иванов <sasha.ivanov@personal.example.com>
```

ID ключа — `ABCD1234`.

### Шаг 2: Добавить GPG-ключ к идентификатору

```json
{
  "gitIdSwitcher.identities": [
    {
      "id": "personal",
      "icon": "🏠",
      "name": "Саша Иванов",
      "email": "sasha.ivanov@personal.example.com",
      "service": "GitHub",
      "description": "Личные проекты",
      "sshKeyPath": "~/.ssh/id_ed25519_personal",
      "gpgKeyId": "ABCD1234"
    }
  ]
}
```

При переключении на этот идентификатор расширение настраивает:

- `git config user.signingkey ABCD1234`
- `git config commit.gpgsign true`

---

## Полный пример: 4 аккаунта с SSH + GPG

Вот полный пример, объединяющий всё:

### Конфигурация SSH (`~/.ssh/config`)

```ssh-config
# Личный аккаунт (по умолчанию)
Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_personal
    IdentitiesOnly yes

# Рабочий аккаунт (Enterprise Managed User от компании)
Host github-work
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_work
    IdentitiesOnly yes

# Аккаунт Bitbucket
Host bitbucket.org
    HostName bitbucket.org
    User git
    IdentityFile ~/.ssh/id_ed25519_bitbucket
    IdentitiesOnly yes
```

### Настройки расширения

```json
{
  "gitIdSwitcher.identities": [
    {
      "id": "personal",
      "icon": "🏠",
      "name": "Саша Иванов",
      "email": "sasha.ivanov@personal.example.com",
      "service": "GitHub",
      "description": "Личные проекты",
      "sshKeyPath": "~/.ssh/id_ed25519_personal",
      "gpgKeyId": "PERSONAL1"
    },
    {
      "id": "work",
      "icon": "💼",
      "name": "Саша Иванов",
      "email": "sasha.ivanov@company.example.com",
      "service": "GitHub Работа",
      "description": "Рабочий аккаунт",
      "sshKeyPath": "~/.ssh/id_ed25519_work",
      "sshHost": "github-work",
      "gpgKeyId": "WORK1234"
    },
    {
      "id": "bitbucket",
      "icon": "🪣",
      "name": "sivanov-bb",
      "email": "sivanov.bb@example.com",
      "service": "Bitbucket",
      "description": "Проекты Bitbucket",
      "sshKeyPath": "~/.ssh/id_ed25519_bitbucket",
      "sshHost": "bitbucket.org"
    },
    {
      "id": "freelance",
      "icon": "🎯",
      "name": "Саша Иванов",
      "email": "sasha.ivanov@freelance.example.com",
      "service": "GitLab",
      "description": "Фриланс-проекты"
    }
  ],
  "gitIdSwitcher.defaultIdentity": "personal",
  "gitIdSwitcher.autoSwitchSshKey": true,
  "gitIdSwitcher.applyToSubmodules": true
}
```

Примечание: Последний идентификатор (`freelance`) без SSH — он только переключает Git-конфигурацию. Это полезно, когда вы используете разную информацию о коммиттере с одним аккаунтом GitHub.

---

## Справочник по конфигурации

### Свойства идентификатора

| Свойство      | Обязательно | Описание                                                   |
| ------------- | ----------- | ---------------------------------------------------------- |
| `id`          | ✅          | Уникальный идентификатор (напр.: `"work"`, `"personal"`)   |
| `name`        | ✅          | Git user.name — отображается в коммитах                    |
| `email`       | ✅          | Git user.email — отображается в коммитах                   |
| `icon`        |             | Эмодзи в статусной строке (напр.: `"🏠"`). Только один эмодзи |
| `service`     |             | Название сервиса (напр.: `"GitHub"`, `"GitLab"`). Используется для отображения в UI |
| `description` |             | Краткое описание в селекторе и подсказке                   |
| `sshKeyPath`  |             | Путь к приватному SSH-ключу (напр.: `"~/.ssh/id_ed25519_work"`) |
| `sshHost`     |             | Алиас SSH-хоста (напр.: `"github-work"`)                   |
| `gpgKeyId`    |             | ID GPG-ключа для подписи коммитов                          |

#### Ограничения отображения

- **Статусная строка**: Текст длиннее ~25 символов будет усечён с `...`
- **`icon`**: Разрешён только один эмодзи (кластер графем). Несколько эмодзи или длинные строки не поддерживаются

### Глобальные настройки

| Настройка                              | По умолчанию | Описание                                       |
| -------------------------------------- | ------------ | ---------------------------------------------- |
| `gitIdSwitcher.identities`             | См. пример   | Список конфигураций идентификаторов            |
| `gitIdSwitcher.defaultIdentity`        | См. пример   | ID идентификатора по умолчанию                 |
| `gitIdSwitcher.autoSwitchSshKey`       | `true`       | Автоматически переключать SSH-ключ             |
| `gitIdSwitcher.showNotifications`      | `true`       | Показывать уведомление при переключении        |
| `gitIdSwitcher.applyToSubmodules`      | `true`       | Применять идентификатор к подмодулям Git       |
| `gitIdSwitcher.submoduleDepth`         | `1`          | Макс. глубина для вложенных подмодулей (1-5)   |
| `gitIdSwitcher.includeIconInGitConfig` | `false`      | Включать эмодзи иконки в Git config `user.name` |
| `gitIdSwitcher.logging.fileEnabled` | `false` | Включить журнал аудита (переключения идентификаторов, операции SSH и т.д.) |
| `gitIdSwitcher.logging.filePath` | `""` | Путь к файлу журнала (напр.: `~/.git-id-switcher/security.log`). Пусто = путь по умолчанию |
| `gitIdSwitcher.logging.maxFileSize` | `10485760` | Макс. размер файла до ротации (байты, 1MB-100MB) |
| `gitIdSwitcher.logging.maxFiles` | `5` | Макс. количество файлов журнала в ротации (1-20) |
| `gitIdSwitcher.logging.level` | `"INFO"` | Уровень логирования: `DEBUG`/`INFO`/`WARN`/`ERROR`/`SECURITY`. Записывает выбранный уровень и выше |
| `gitIdSwitcher.commandTimeouts` | `{}` | Пользовательский таймаут для команд (мс, 1сек-5мин). Напр.: `{"git": 15000, "ssh-add": 10000}` |

#### О настройке `includeIconInGitConfig`

Управляет поведением при установленном поле `icon`:

| Значение | Поведение |
|----------|-----------|
| `false` (по умолчанию) | `icon` отображается только в интерфейсе редактора. Только `name` записывается в Git config |
| `true` | `icon + name` записывается в Git config. Эмодзи появляется в истории коммитов |

Пример: `icon: "👤"`, `name: "Саша Иванов"`

| includeIconInGitConfig | Git config `user.name` | Подпись коммита |
|------------------------|------------------------|-----------------|
| `false` | `Саша Иванов` | `Саша Иванов <email>` |
| `true` | `👤 Саша Иванов` | `👤 Саша Иванов <email>` |

### Примечание: Базовая настройка (без SSH)

Если вам не нужно переключать SSH-ключи (напр., при использовании разной информации о коммиттере с одним аккаунтом GitHub), можно использовать минимальную конфигурацию:

```json
{
  "gitIdSwitcher.identities": [
    {
      "id": "personal",
      "icon": "🏠",
      "name": "Саша Иванов",
      "email": "sasha.ivanov@personal.example.com",
      "description": "Личные проекты"
    },
    {
      "id": "work",
      "icon": "💼",
      "name": "Саша Иванов",
      "email": "sasha.ivanov@company.example.com",
      "description": "Рабочий аккаунт"
    }
  ]
}
```

Эта конфигурация переключает только `git config user.name` и `user.email`.

---

## Как это работает

### Структура уровней Git Config

Конфигурация Git имеет три уровня, где нижние уровни переопределяют верхние:

```text
Системный (/etc/gitconfig)
    ↓ переопределяет
Глобальный (~/.gitconfig)
    ↓ переопределяет
Локальный (.git/config)  ← высший приоритет
```

**Git ID Switcher записывает в `--local` (локально для репозитория).**

Это означает:

- Идентификатор сохраняется в `.git/config` каждого репозитория
- Можно поддерживать разные идентификаторы для каждого репозитория
- Глобальные настройки (`~/.gitconfig`) не изменяются

### При переключении идентификатора

При переключении идентификатора расширение выполняет (по порядку):

1. **Git-конфигурация** (всегда): Устанавливает `git config --local user.name` и `user.email`
2. **SSH-ключ** (если задан `sshKeyPath`): Удаляет другие ключи из ssh-agent, добавляет выбранный
3. **GPG-ключ** (если задан `gpgKeyId`): Устанавливает `git config --local user.signingkey` и включает подпись
4. **Подмодули** (если включено): Применяет конфигурацию ко всем подмодулям (по умолчанию: глубина 1)

### Как работает распространение на подмодули

Локальные настройки применяются к каждому репозиторию отдельно, поэтому они не применяются автоматически к подмодулям.
Поэтому это расширение предоставляет функцию распространения на подмодули (см. «Продвинутое: Поддержка подмодулей» для подробностей).

---

## Продвинутое: Поддержка подмодулей

Для сложных репозиториев с Git-подмодулями управление идентификатором часто вызывает проблемы. Если вы делаете коммит в подмодуле, Git использует локальную конфигурацию этого подмодуля, которая может использовать глобальную конфигурацию (неправильный email!), если не настроена явно.

**Git ID Switcher** автоматически обнаруживает подмодули и применяет к ним выбранный идентификатор.

```json
{
  "gitIdSwitcher.applyToSubmodules": true,
  "gitIdSwitcher.submoduleDepth": 1
}
```

- `applyToSubmodules`: Включить/отключить эту функцию
- `submoduleDepth`: Насколько глубоко идти?
  - `1`: Только прямые подмодули (наиболее частый случай)
  - `2+`: Вложенные подмодули (подмодули внутри подмодулей)

Это гарантирует, что ваш идентификатор всегда правильный, независимо от того, делаете ли вы коммит в основном репозитории или в vendor-библиотеке.

---

## Устранение неполадок

### SSH-ключ не переключается?

1. Убедитесь, что `ssh-agent` запущен:

   ```bash
   eval "$(ssh-agent -s)"
   ```

2. Проверьте правильность пути к ключу:

   ```bash
   ls -la ~/.ssh/id_ed25519_*
   ```

3. На macOS добавьте в Keychain один раз:

   ```bash
   ssh-add --apple-use-keychain ~/.ssh/id_ed25519_work
   ```

### Неправильный идентификатор при push?

1. Проверьте, что удалённый URL использует правильный алиас хоста:

   ```bash
   git remote -v
   # Должен показывать git@github-work:... для рабочих репозиториев
   ```

2. Обновите при необходимости:

   ```bash
   git remote set-url origin git@github-work:company/repo.git
   ```

### GPG-подпись не работает?

1. Найдите ID вашего GPG-ключа:

   ```bash
   gpg --list-secret-keys --keyid-format SHORT
   ```

2. Протестируйте подпись:

   ```bash
   echo "test" | gpg --clearsign
   ```

3. Убедитесь, что email в вашем идентификаторе совпадает с email GPG-ключа.

### Идентификатор не определяется?

- Убедитесь, что вы находитесь в Git-репозитории
- Проверьте `settings.json` на синтаксические ошибки
- Перезагрузите окно VS Code (`Cmd+Shift+P` → "Перезагрузить окно")

### Ошибка с полем `name`?

Следующие символы в поле `name` вызовут ошибку:

`` ` `` `$` `(` `)` `{` `}` `|` `&` `<` `>`

Используйте поле `service`, если хотите включить информацию о сервисе.

```jsonc
// NG
"name": "Саша Иванов (Личный)"

// OK
"name": "Саша Иванов",
"service": "GitHub"
```

### Новые настройки не отображаются?

После обновления расширения новые настройки могут не отображаться в интерфейсе настроек.

**Решение:** Полностью перезагрузите компьютер.

Редакторы на базе VS Code кэшируют схему настроек в памяти, и «Перезагрузить окно» или переустановка расширения может быть недостаточно для её обновления.

### Значения по умолчанию пустые?

Если примеры настроек не появляются даже после чистой установки, причиной может быть **Settings Sync**.

Если вы ранее сохранили пустые настройки, они могли синхронизироваться в облако и перезаписывать значения по умолчанию при новых установках.

**Решение:**

1. Найдите настройку в интерфейсе настроек
2. Нажмите на иконку шестерёнки → «Сбросить настройку»
3. Синхронизируйте с Settings Sync (это удалит старые настройки из облака)

---

## Команды

| Команда                         | Описание                          |
| ------------------------------- | --------------------------------- |
| `Git ID: Select Identity`       | Открыть селектор идентификаторов  |
| `Git ID: Show Current Identity` | Показать текущий идентификатор    |

---

## Философия дизайна

> «Кто я?» — Единственный вопрос, на который отвечает это расширение.

Построено на **Архитектуре Карэсансуй**: простое ядро (100 строк),
окружённое намеренным качеством (90% покрытие, логирование, таймауты)
и осознанными ограничениями (без GitHub API, без управления токенами).

[![Karesansui Architecture](https://img.shields.io/badge/🪨_Karesansui-Architecture-4a5568)](../../DESIGN_PHILOSOPHY.md)

[Читать полную философию](../../DESIGN_PHILOSOPHY.md)

---

## Участие в разработке

Вклад приветствуется! См. [CONTRIBUTING.md](../../CONTRIBUTING.md).

## Лицензия

Лицензия MIT — см. [LICENSE](../../../LICENSE).

## Благодарности

Создано [Null;Variant](https://github.com/nullvariant)
